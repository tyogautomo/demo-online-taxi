import { connect } from 'react-redux';

import { SearchPlace } from './SearchPlace.screen';

const mapStatToProps = state => ({});
const mapDispatchToProps = dispatch => ({});

export default connect(
  mapStatToProps,
  mapDispatchToProps
)(SearchPlace);